type Query {
  menu: [MenuItem!]!
}

type Mutation {
  createItem(
    name: String!
    order: Int!
    hidden: Boolean!
  ): MenuItem
  createColumn(
    id: ID!
    name: String!
    order: Int!
  ): MenuColumn
  createTarget(
    id: ID!
    target: ID!
    order: Int!
  ): MenuTarget
}
